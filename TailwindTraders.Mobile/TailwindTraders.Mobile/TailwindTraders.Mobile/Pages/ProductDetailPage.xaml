<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:TailwindTraders.Mobile.ViewModels"
             mc:Ignorable="d"
             x:Class="TailwindTraders.Mobile.Pages.ProductDetailPage"
             Visual="Material">
    <ContentPage.BindingContext>
        <viewmodels:ProductDetailViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding ProductName}" />
            <Label Text="{Binding Price}"/>
            <Label Text="{Binding ImageUrl}"/>
            <Label Text="{Binding CategoryCode}"/>
            <Label Text="{Binding QuantityRemaining}"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>